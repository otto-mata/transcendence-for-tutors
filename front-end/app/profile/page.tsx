import ProfilePageClient from '@/components/ProfilePageClient';

export default function MyProfilePage() {
	// This is the current user's own profile
	return <ProfilePageClient username="" isOwnProfile={true} />;
}
